# 実験に関する申し送り

このファイルには「コードを読んでも分からない、実験結果の解釈に関わる事実」だけを書く。
コードの構成は `REFACTOR_PLAN.md`、日々の進捗は `PROJECT_STATUS.md` を見ること。

最終更新: 2026-07-29（リポジトリ移行 Phase 4 時点）

---

## 1. ABMIL の数値は、リーク修正の前後で比較できない

`lib/evaluate/abmil.py` の early stopping が、**val fold を使って停止エポックを選び、
その同じ val fold で AUC を報告していた**（旧 `scripts/evaluate/abmil_eval.py:166-212` と
`main()` の組み合わせ。REFACTOR_PLAN.md §3-2）。この AUC は楽観バイアスを含む。
さらに、比較対象の mean-pool ベースラインは同じ選択を受けていないため、
**ABMIL 対 mean-pool の比較自体が不公平**だった。

- Phase 1-c でコード側は修正済み。
- **過去に出した数値は再計算しない**という決定（§10-5）。
- したがって、**修正前に出た ABMIL の数値を論文にそのまま使ってはいけない**。
  使うなら再計算すること。
- 旧挙動は `--early_stop` で再現できるが、その AUC は上記のバイアス込みである。

## 2. 旧実験の出力は `result/` 配下に凍結されている

結果の置き場は `outputs/` に統一した（§5-1）。ただし移行前の学習は
`result/` 配下に書いており、**これらは `outputs/` へ集約せずそのまま凍結**する
という決定（§10-8）。

- `result/` へ新規に書き込むものは無い。
- 過去の実験結果を探すときは `result/` も見ること。
- `result/` `outputs/` はいずれも `.gitignore` 対象なのでリポジトリには入らない。

なお `--dir_result` の二重ネストバグ（§1-3）を修正したため、
`result/` の中には `result/workspace/andre01/honzawa/wsi-ad/outputs/...` という
異常に深いパスが残っている可能性がある。これはバグの痕跡であって、意味は無い。

## 3. 再実行できなくなった実験が5件ある

Phase 0 の削除と Phase 1-b の移設により、以下の実験は
**現在の HEAD ではそのまま再実行できない**（§4-1、§10-7 で受容と決定）。
再現したい場合は `c5b3ebd` 以前をチェックアウトすること。

| 実験 | 理由 |
|---|---|
| `20260507_190600_train_test` | `scripts/tggate/` を削除 |
| `20260508_122551_new_test` | 同上 |
| `20260508_162317_wandb_test` | 同上 |
| `20260509_231646_some_learn_cifar` | 同上（`train_cifar.py` も削除） |
| `20260519_135532_tggate_test` | `run_slurm.sh:46` が `rsync ${SUBMIT_DIR}/src` をしている（`src/` は `lib/` へ移設済み） |

「既存 `run_slurm.sh` は書き換えない」方針（§4）に従い、これらには手を入れていない。

## 4. 移行前後で挙動が一致することの実測保証は無い

Phase -1 で取るはずだったベースライン（学習1本を流して移行前後の出力を突き合わせる）は
**ユーザー判断でスキップされた**（§1-4 / §10-2）。
このため Phase 0 以降の各フェーズの完了条件は、実測比較ではなく
**静的検証（`compileall` / dangling reference の grep / AST による引数整合チェック）**に
置き換わっている。

- 移行後に最初に学習を1本流すときは、これが実質的な初回の動作確認になる。
  loss の初期値・1エポックの所要時間が過去の実験と大きくずれていないかを見ること。
- 特に `OMP_NUM_THREADS=1` が効いているかは要注意（§1-1）。
  これが落ちると augmentation が約16倍遅くなる。
  `scripts/slurm_entry.sh` がコンテナ内へ引き継いでいる。

## 5. 実験ディレクトリの命名が2系統ある

- 移行前の31件: `YYYYMMDD_HHMMSS_name` / `YYYYMMDD_paper_name`
- Phase 2 以降の新規: `NNNN_YYYYMMDD_name`（`make create_exp` が採番）

`runx` / `cdx` / `lsx` / `make preflight` はいずれも `^[0-9]{4}_` を前提にしているため、
**旧31件はこれらの自動検出に出てこない**（§5-2）。これは意図した挙動で、
旧実験は凍結記録として扱う。旧実験を見たいときは `ls experiments/` を使う。
