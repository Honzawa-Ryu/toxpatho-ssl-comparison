# 作業ルール

## ⚠️ 実行環境の使い分け（最重要）

**この環境（Miyabi）は従量課金。実行は極力避ける。**

| | やる場所 |
|---|---|
| **SSL 事前学習**（マルチノードGPU、日単位） | **Miyabi = ここ** |
| **評価・解析のすべて** | **andre01 = ユーザーの計算機** |

「評価・解析」には次が含まれる。**Miyabi では回さない。**

- 特徴抽出（`extract_features.py` など、凍結エンコーダでの推論）
- 表現比較（埋め込み抽出 / CKA / η² / k-means / t-SNE、`lib/analysis/*`）
- 下流評価（線形プローブ、ABMIL、用量反応、所見別 AUROC）

### エージェントの振る舞い

- **`qsub` / `sbatch` を勝手に投げない。** 学習ジョブであっても投入前に必ず確認を取る。
- 評価・解析のコードは**書いてコミットするところまで**をここでの仕事とする。
  実行はユーザーが向こうで行う。「動かして確かめましょうか」と提案しない。
- ログインノードでの軽い検証（import 確認、単体テスト、コンテナ内の CPU スモークテスト、
  チェックポイントの読み込み確認）は課金対象の計算ノードを使わないので行ってよい。
  判断に迷ったら「計算ノードを使うか」を基準にする。
- 結果として、このリポジトリのコミットには「実装済み・未実行」の状態が普通に含まれる。
  それは未完了ではなく想定どおりの受け渡し形態。

## リポジトリの関係

| リポジトリ | 役割 |
|---|---|
| `toxpatho-ssl-comparison`（ここ） | SSL 手法比較の本体。学習コードと表現比較パイプライン |
| `toxpatho-uni` | 基盤モデル(UNI)比較と**下流評価基盤**。所見ラベル・マニフェスト・評価スクリプト |

下流評価の資産は `toxpatho-uni` 側にある（`data/manifest.csv`、
`scripts/eval_by_finding.py`、UNI / UNI2-h / ImageNet-ViT のベースライン結果）。
本リポジトリで学習したチェックポイントを評価するときは、そちらの
`scripts/extract_features.py --encoder dino --dino-ckpt <ckpt> --name <名前>` を使う。

## ドキュメントの真実源

- `Goal.yaml` — 目的・方針の単一の真実源。**方針変更時はまずここを更新**してから実装を直す
- `PROJECT_STATUS.md` — 経緯と現状。**毎回の作業後に必ず更新**。セッション開始時にまず読む
- `TODO.md` — やること・研究方針の一覧

## 用語の注意

「論文準拠」と一語で書かない。DINO は**論文本文と公式コード既定が食い違う**箇所が
あるため（teacher温度 / local crops 数 / clip_grad）、どちらに準拠したのかを明示すること。
詳細は `PROJECT_STATUS.md`「◆ 論文本文と公式コードが食い違う3点」。
